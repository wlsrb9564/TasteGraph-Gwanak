services:
  neo4j:
    image: neo4j:5.26.26
    container_name: matjip-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - NEO4J_AUTH=neo4j/${NEO4J_PASSWORD}
    restart: unless-stopped
