[project]
name = "tastegraph-gwanak"
version = "0.1.0"
description = "관악구 맛집 추천 서비스 — Neo4j KG 기반 자연어 검색"
requires-python = ">=3.13"
dependencies = [
    "neo4j>=5.0",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
